[project]
name = "tt-news"
version = "0.1.0"
description = "资讯模块 — 新闻、公告、全文检索"
requires-python = ">=3.11"
dependencies = [
    "pyside6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/news"]
